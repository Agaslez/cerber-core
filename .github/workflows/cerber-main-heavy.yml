# AUTO-GENERATED BY CERBER — DO NOT EDIT

name: Cerber Heavy Verification (Main)

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  NODE_VERSION: 20
  INIT_TIMEOUT_SECONDS: 90

jobs:
  lint_and_typecheck:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      - run: npm ci
      - run: npm run lint || echo "⚠️  ESLint skipped"
      - run: npx tsc --noEmit

  build_and_unit:
    name: Build & Unit Tests (@fast)
    runs-on: ubuntu-latest
    needs: lint_and_typecheck
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      - run: chmod +x bin/*.cjs bin/cerber* || true
      - run: npm ci
      - run: npm run build
      - run: npm run test:fast
        env:
          CERBER_TEST_MODE: "1"

  integration_tests:
    name: Integration Tests (@integration)
    runs-on: ubuntu-latest
    needs: build_and_unit
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      - run: npm ci
      - run: npm run build
      - run: npm run test:integration
        env:
          CERBER_TEST_MODE: "1"

  e2e_tests:
    name: E2E Tests (@e2e)
    runs-on: ubuntu-latest
    needs: build_and_unit
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      - run: npm ci
      - run: npm run build
      - run: npm run test:e2e
        env:
          CERBER_TEST_MODE: "1"

  signals_tests:
    name: Signal Handling Tests (@signals)
    runs-on: ubuntu-latest
    needs: build_and_unit
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      - run: npm ci
      - run: npm run build
      - run: npm run test:signals
        env:
          CERBER_TEST_MODE: "1"

  summary:
    name: All Checks Summary
    runs-on: ubuntu-latest
    needs:
      - lint_and_typecheck
      - build_and_unit
      - integration_tests
      - e2e_tests
      - signals_tests
    if: always()
    steps:
      - run: |
          if [[ "${{ needs.lint_and_typecheck.result }}" != "success" ]] || \
             [[ "${{ needs.build_and_unit.result }}" != "success" ]] || \
             [[ "${{ needs.integration_tests.result }}" != "success" ]] || \
             [[ "${{ needs.e2e_tests.result }}" != "success" ]] || \
             [[ "${{ needs.signals_tests.result }}" != "success" ]]; then
            echo "❌ One or more checks failed"
            exit 1
          fi
          echo "✅ All checks passed"