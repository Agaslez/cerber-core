# AUTO-GENERATED BY CERBER — DO NOT EDIT

name: Cerber Fast Checks (PR)

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  NODE_VERSION: 20

jobs:
  lint_and_typecheck:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      - run: npm ci
      - run: npm run lint || echo "⚠️  ESLint skipped"
      - run: npx tsc --noEmit

  build_and_test:
    name: Build & Tests (@fast + @integration)
    runs-on: ubuntu-latest
    needs: lint_and_typecheck
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      - run: chmod +x bin/*.cjs bin/cerber* || true
      - run: npm ci
      - run: npm run build
      - run: npm run test:ci:pr
        env:
          CERBER_TEST_MODE: "1"

  cerber_integrity:
    name: Cerber Integrity (Protected Files)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Full history to compare against main
      - name: Verify protected files have owner approval
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REQUIRED_OWNER: owner
        run: node bin/cerber-integrity.cjs

  pr_summary:
    name: PR FAST (required)
    runs-on: ubuntu-latest
    needs: [lint_and_typecheck, build_and_test, cerber_integrity]
    if: always()
    steps:
      - run: |
          if [[ "${{ needs.lint_and_typecheck.result }}" != "success" ]] || \
             [[ "${{ needs.build_and_test.result }}" != "success" ]] || \
             [[ "${{ needs.cerber_integrity.result }}" != "success" ]]; then
            echo "❌ Fast gate failed"
            exit 1
          fi
          echo "✅ All fast checks passed"