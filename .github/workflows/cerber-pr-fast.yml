# AUTO-GENERATED BY CERBER — DO NOT EDIT

name: Cerber Fast Checks (PR)

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  NODE_VERSION: 20

jobs:
  lint_and_typecheck:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      - run: npm ci
      - run: npm run lint || echo "⚠️  ESLint skipped"
      - run: npx tsc --noEmit

  build_and_unit:
    name: Build & Unit Tests (@fast)
    runs-on: ubuntu-latest
    needs: lint_and_typecheck
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      - run: chmod +x bin/*.cjs bin/cerber* || true
      - run: npm ci
      - run: npm run build
      - run: npm run test:fast
        env:
          CERBER_TEST_MODE: "1"

  pr_summary:
    name: PR Summary
    runs-on: ubuntu-latest
    needs: [lint_and_typecheck, build_and_unit]
    if: always()
    steps:
      - run: |
          if [[ "${{ needs.lint_and_typecheck.result }}" != "success" ]] || \
             [[ "${{ needs.build_and_unit.result }}" != "success" ]]; then
            echo "❌ Fast gate failed"
            exit 1
          fi
          echo "✅ All fast checks passed"